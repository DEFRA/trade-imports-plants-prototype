# To set up Sonar cloud follow https://github.com/DEFRA/cdp-documentation/blob/main/how-to/sonarcloud.md
# Update properties in this file as per the howto instructions
# Uncomment properties in this .github/workflows/check-pull-request.yml
# Uncomment properties in this .github/workflows/publish.yml
# Uncomment properties in this .github/workflows/publish-hotfix.yml
#
# Until those steps are uncommented the analysis is SonarQube Cloud's Automatic
# Analysis, which ignores this file and reads .sonarcloud.properties. The scope
# and duplication settings below are mirrored there; change one, change both.

sonar.projectKey=DEFRA_trade-imports-plants-frontend
sonar.organization=defra

sonar.links.homepage=https://github.com/DEFRA/trade-imports-plants-frontend
sonar.links.ci=https://github.com/DEFRA/trade-imports-plants-frontend/actions
sonar.links.scm=https://github.com/DEFRA/trade-imports-plants-frontend
sonar.links.issue=https://github.com/DEFRA/trade-imports-plants-frontend/issues

sonar.sources=src/
sonar.exclusions=src/**/*.test.js,src/**/*.fit.spec.js
sonar.tests=src/
sonar.test.inclusions=src/**/*.test.js,src/**/*.fit.spec.js

sonar.javascript.lcov.reportPaths=./coverage/lcov.info

# Browser bundles — vitest cannot execute them and the Playwright suites
# prove them. Still analysed for issues, just not measured for coverage.
sonar.coverage.exclusions=src/client/**

# copy.en.js / copy.cy.js are parallel language decks and the address-book
# stubs are canned party records — both are duplicate-shaped by design.
sonar.cpd.exclusions=**/config.js,src/**/*.test.js,src/**/*.fit.spec.js,src/**/copy.??.js,src/server/app/services/address-book/stub/**

# readDate returns a parsed date or passes the raw string through for
# redisplay; callers branch on typeof, so a single return type is not possible.
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=javascript:S3800
sonar.issue.ignore.multicriteria.e1.resourceKey=src/server/app/shared/kit.js
