/**
 * The answer-tree names this feature writes.
 *
 * Every one of them is a manifest obligation name. CATEGORY, GENUS, QUANTITY
 * and POTATO_VARIETY are also the DOM field names on the entry page; LINES is
 * the collection the group writes into. Naming them once keeps the group's own
 * modules — and the commodity-type page, which reconciles the group when the
 * notification's type changes — reading the same key.
 */
export const LINES = 'commodityLines'
export const CATEGORY = 'category'
export const GENUS = 'genus'
export const QUANTITY = 'quantity'
export const POTATO_VARIETY = 'potatoVariety'
