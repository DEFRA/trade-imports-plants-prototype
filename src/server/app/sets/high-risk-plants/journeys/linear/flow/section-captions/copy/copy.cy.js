// MACHINE-DRAFT Welsh — not reviewed by a translator. Do not ship user-facing without Welsh Language Standards sign-off.
// The dashboard caption is spec-sourced (journey-spec.json
// pages[dashboard].captionCy); `aboutTheConsignment` is the live-animals
// caption for the identical English string. `arrival` is the noun the spec
// itself uses for arrival in Welsh — pages[arrival-details].titleCy is
// "Manylion cyrraedd". `destination` is the noun the spec uses for destination
// in Welsh — pages[hub].groupsCy names group 2 "Cyrraedd a chyrchfan", and
// pages[place-of-destination].titleCy is "Man cyrchfan". A section added later
// must arrive with its Welsh string; do not invent one.
export const copy = {
  sections: {
    consignmentParties: 'Partïon y llwyth',
    dashboard: 'Dangosfwrdd',
    aboutTheConsignment: 'Am y llwyth',
    arrival: 'Cyrraedd',
    destination: 'Cyrchfan'
  }
}
